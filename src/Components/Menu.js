import React from "react";
const Menu  = (props) => {
    return (  
        <mav>
            <p>{props.titulo}</p>
            <p>{props.subtitulo}</p>
        </mav>
    );
}
 
export default Menu;